apiVersion: v1
kind: Service
metadata:
  name: aceest-service
spec:
  selector:
    app: aceest
    version: blue  # Initially pointing to blue deployment
  ports:
    - port: 80
      targetPort: 5000
  type: NodePort